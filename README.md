# Alzheimer Risk Prediction App

This is a Flask-based web app + REST API for predicting Alzheimer’s risk using a trained RandomForest model.

## Local Run
```bash
pip install -r requirements.txt
python app.py
